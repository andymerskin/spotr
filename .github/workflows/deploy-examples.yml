name: Deploy Examples Site

on:
  push:
    branches:
      - master
    paths:
      - 'site/**'
      - 'examples/**'
      - 'packages/spotr/package.json'
      - '.github/workflows/deploy-examples.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      - name: Cache Bun dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install

      - name: Build spotr library
        run: bun run --filter spotr build

      - name: Cache example dependencies
        id: cache-examples
        uses: actions/cache@v4
        with:
          path: examples/*/*/node_modules
          key: ${{ runner.os }}-examples-deps-${{ hashFiles('examples/**/package.json', 'examples/**/package-lock.json', 'examples/**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-examples-deps-

      - name: Install example dependencies
        if: steps.cache-examples.outputs.cache-hit != 'true'
        run: bun run examples:install

      - name: Typecheck examples
        run: bun run examples:typecheck

      - name: Typecheck site
        working-directory: site
        run: bun run typecheck

      - name: Build Astro site
        working-directory: site
        env:
          PUBLIC_STACKBLITZ_BRANCH: master
        run: bun run build

      - name: Prepare output directory
        run: |
          mkdir -p deploy-output
          # Use . to copy contents including hidden files, handle empty directories
          if [ -d "site/dist" ] && [ "$(ls -A site/dist 2>/dev/null)" ]; then
            cp -r site/dist/. deploy-output/ 2>/dev/null || cp -r site/dist/* deploy-output/
          else
            echo "Warning: site/dist is empty or missing"
          fi

      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: deploy-output
          name: pages-artifact

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: pages-artifact
      - name: Output deployment URL
        run: |
          echo "Deployed to: ${{ steps.deployment.outputs.page_url }}"
